export default function Rezultate() {
    return (
        <main style={{ padding: "40px" }}>
            <h1>Rezultate 🎨</h1>

            <p>Aici va apărea design-ul generat de AI.</p>

            <div
                style={{
                    marginTop: "20px",
                    height: "300px",
                    background: "#eee",
                    borderRadius: "12px",
                    display: "flex",
                    alignItems: "center",
                    justifyContent: "center",
                }}
            >
                🛋️ Design generat aici
            </div>
        </main>
    );
}